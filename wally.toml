[package]
name = "2jammers/profilestore"
description = "Periodic DataStore saving solution with session locking"
version = "0.1.0"
license = "Apache 2.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

[dependencies]
